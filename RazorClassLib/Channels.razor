<MudList Clickable="true">
    @{
        if (ChannelList is null)
        {
            <ChannelLoading/>
        }
        foreach (var channel in ChannelList)
        {
            <MudListItem Dense="true" OnClick="() => LoadChannelChat.InvokeAsync(channel)"># @channel.Name</MudListItem>
        }
    }
</MudList>

@code
{
    [Parameter] public List<Channel> ChannelList { get; set; }
    [Parameter] public EventCallback<Channel> LoadChannelChat { get; set; }
}
